// Task: Create two files, src/math.ts and src/index.ts. Export a function from math.ts and import it into index.ts.

// Create src/math.ts:

// // src/math.ts
// export function add(a: number, b: number): number {
//   return a + b;
// }
// Create src/index.ts:

// // src/index.ts
// import { add } from './math'; // Note the relative path

// const result = add(5, 10);
// console.log(`The result is: ${result}`);
// Run it: Execute pnpm tsx src/index.ts from your terminal.
