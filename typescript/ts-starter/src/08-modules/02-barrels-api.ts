// Create src/shared/index.ts that re-exports ./math, ./strings.
// Import from @shared paths configured via tsconfig.paths.

// import { add, capitalize } from "@shared";
import { add, capitalize } from "@shared/index";