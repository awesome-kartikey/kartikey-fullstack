function greet1(name: string) {
  return `Hello, ${name}!`;
}

console.log(greet1("TypeScript"));

const fullName = "Govinda"; // Use F2 to rename all occurence of "fullName"
console.log(fullName);

const fullName2 = "Govinda";
console.log(fullName2);
const greeting1 = `Hello ${fullName2}`;

const price1 = 100;
const tax = price1 * 0.1;
const total = price1 + tax;
console.log(total);

console.log(":");
