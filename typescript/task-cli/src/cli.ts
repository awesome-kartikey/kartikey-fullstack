// New functionality: - Full CLI interface - Interactive mode - Rich formatting and colors - Configuration management
// Using commander, inquirer, chalk
// export class TaskCLI {
//   constructor(private manager: TaskManager);

//   async run(args: string[]): Promise<void>;

//   private setupCommands(): void;
//   private interactive(): Promise<void>;
// }
